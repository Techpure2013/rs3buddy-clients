{
  "name": "@rs3buddy/client",
  "version": "0.1.0",
  "description": "rs3buddy HTTP client (TypeScript) — typed wrapper over the rs3buddy server.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist", "src"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test test/transport.test.mjs test/client.test.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.15.21",
    "typescript": "^5.9.3"
  },
  "license": "UNLICENSED"
}
